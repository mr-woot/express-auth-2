const SERVICES = {
  SERVICE_A: "service_a",
  SERVICE_B: "service_b",
  SERVICE_C: "service_c"
};
Object.freeze(SERVICES);

const ROLES = {
  ADMIN: "admin",
  USER: "user"
};

Object.freeze(ROLES);

module.exports = { SERVICES, ROLES };
